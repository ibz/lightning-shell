#!/bin/bash

if [ "$1" == 'describe' ]; then
        exit 0
fi

echo "Lightning Shell `cat /home/lnshell/.lnshell-version` by @ibz / https://ibz.me"
echo "Read more at https://ibz.github.io/lightning-shell/"
echo "Pull Requests to add / update utilities are very welcome!"
