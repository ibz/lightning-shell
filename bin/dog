#!/bin/bash

if [ "$1" == 'describe' ]; then
        echo 'A command-line DNS client.'
        exit 0
fi

if [ "$1" == 'url' ]; then
        echo "https://github.com/ogham/dog"
        exit 0
fi

/bin/dog $@
