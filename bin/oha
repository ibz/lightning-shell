#!/bin/bash

if [ "$1" == 'describe' ]; then
        echo 'HTTP load generator with TUI animation.'
        exit 0
fi

if [ "$1" == 'url' ]; then
        echo "https://github.com/hatoo/oha"
        exit 0
fi

/bin/oha $@
