#!/bin/bash

if [ "$1" == 'describe' ]; then
        echo 'Ping, but with a graph.'
        exit 0
fi

if [ "$1" == 'url' ]; then
        echo "https://github.com/orf/gping"
        exit 0
fi

/bin/gping $@
